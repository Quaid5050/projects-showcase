# Optional for local dev (npm start uses the "proxy" field in package.json instead).
# REQUIRED when deploying to Vercel — set this in Vercel Project Settings -> Environment Variables
# to your deployed backend's public URL, e.g. https://pb-exotics-backend.onrender.com/api
REACT_APP_API_URL=http://localhost:5000/api

# MongoDB (required for bookings)
MONGODB_URI=mongodb://localhost:27017/blacktrucks

# Stripe — use test keys only until live keys + webhook secret are provided
STRIPE_SECRET_KEY=your_stripe_secret_key_here
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=STRIPE_SECRET_KEY=your_stripe_publishbale_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Site URL for Stripe redirects (no trailing slash)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# NextAuth / email / SMTP — see README or existing deployment docs

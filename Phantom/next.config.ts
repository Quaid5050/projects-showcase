import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Local assets live in /public/placeholders. Add remotePatterns here if you switch back to hosted images. */
};

export default nextConfig;

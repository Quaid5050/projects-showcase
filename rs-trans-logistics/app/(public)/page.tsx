// Server component — renders the dynamic client homepage (no SSR for animations)
import HomePageWrapper from '@/components/HomePageWrapper';

export default function HomePage() {
  return <HomePageWrapper />;
}
